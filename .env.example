# SMTP Configuration for sending reminder emails
# Example for Gmail: use an App Password (not your regular password)
# Generate at: https://myaccount.google.com/apppasswords

SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Manager email — reminder emails are sent TO this address
ADMIN_EMAIL=manager@example.com

# Server port (optional, defaults to 3000, ignored on Vercel)
# PORT=3000
